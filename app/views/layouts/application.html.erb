<!DOCTYPE html>
<html>
<head>
  <title>IC-UFF Controle de Salas</title>
  
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  
  <link href='https://fonts.googleapis.com/css?family=Roboto:300' rel='stylesheet' type='text/css'>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class = "icone">
    <div class="container header-logo-default">
      <%= link_to image_tag("IC-Logo.png", alt: "Rails logo"), root_path%>
    </div>
  </div>
      
  <nav class="navbar navbar-default">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
      </div>
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <li><%= link_to "Salas", salas_path%></a></li>
          <% if logged_in? %>
            <li><%= link_to "Reserva de Salas", reserva_salas_path%></a></li>
          <% end %>
          <li><%= link_to "Equipamentos", equipamentos_path%></a></li>
          <% if logged_in? %>
          <% if logged_in? %>
            <li><%= link_to "Reserva de Equipamentos", reserva_equipamentos_path%></a></li>
          <% end %>
            <% if current_user.gerente? && !current_user?(@user) %>
              <li><%= link_to "Usuários", usuarios_path%></a></li>
            <% end %>
          <% end %>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <% if logged_in? %>
          <li><%= link_to "Usuário", current_user %></li>
          <li><%= link_to "Sair", logout_path, method: "delete" %></li>
          <% else %>
          <li><%= link_to "Login", login_path %></li>
          
          <% end %>
          <% if logged_in? %>
            <% if current_user.gerente? && !current_user?(@user) %>
            <li><%= link_to "Cadastrar Novo Usuário", cadastro_path %></li>
            <% end %>
          <% end %>
        </ul>


        </div><!-- /.navbar-collapse -->
      </div><!-- /.container-fluid -->
    </nav>
  </div>
  <div class="container">
    <%= yield %>
  </div>
  <footer id = "footer">
      <p><br>
      Desenvolvido por IN Junior
      <br>
      <br> </p>
  </footer>

     
        

  
</body>

</html>
